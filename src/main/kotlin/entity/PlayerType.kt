package entity

/**
 * Enum to distinguish between player types
 * A player can be controlled by a HUMAN, or can be an EASYBOT or a HARDBOT
 */

enum class PlayerType {
    HUMAN,
    EASYBOT,
    HARDBOT
}
